language: java
script: ./gradlew clean build -x test
deploy:
  provider: releases
  api_key: "${GITHUB_KEY}"
  file: "*/build/libs/kafka-consumer-lag-monitoring-*.jar "
  skip_cleanup: true
  on:
    tags: false